#ifndef NUMBER_LINES_CHAR_H_
#define NUMBER_LINES_CHAR_H_

#include "fileUtils.h"

// Foction de vérification du nombre de lignes dans le fichier, elle prend en paramètre le path du fichier.
void checkLinesNumbers(char*);
// Fonction de vérification du nombre de caractères par lignes, elle prend en paramètre le path du fichier.
void checkCharactersNumbers(char*);

#endif